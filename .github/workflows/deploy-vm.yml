name: Deploy to VM

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          if [ -z "${{ secrets.VM_SSH_HOST }}" ] || [ -z "${{ secrets.VM_SSH_PORT }}" ] || [ -z "${{ secrets.VM_SSH_USER }}" ] || [ -z "${{ secrets.VM_SSH_KEY }}" ]; then
            echo "Missing one or more required secrets: VM_SSH_HOST, VM_SSH_PORT, VM_SSH_USER, VM_SSH_KEY"
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s' "${{ secrets.VM_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p "${{ secrets.VM_SSH_PORT }}" -H "${{ secrets.VM_SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy
        run: |
          set -o pipefail
          ssh -i ~/.ssh/id_rsa -p "${{ secrets.VM_SSH_PORT }}" "${{ secrets.VM_SSH_USER }}@${{ secrets.VM_SSH_HOST }}" "bash /opt/radar-warroom/Smart-Investment-stretegy/infra/vm/deploy.sh" | tee deploy.log

      - name: Deploy log summary (on failure)
        if: failure()
        run: |
          echo "==== deploy log (last 200 lines) ===="
          if [ -f deploy.log ]; then
            tail -n 200 deploy.log
          else
            echo "No deploy log captured."
          fi

