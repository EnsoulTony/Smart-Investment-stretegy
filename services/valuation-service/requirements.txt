# valuation-service 專屬依賴
# Sprint 1-4.B：估值層骨架，嚴禁 DB 直連

# HTTP client（取代 SQLAlchemy）
httpx>=0.25.0,<1.0.0

# 數據驗證
# 註：pydantic 版本由 fastapi 決定，避免版本衝突

# ❌ 禁止清單（由 Guardrails 測試檢查）：
# - sql + alchemy
# - psy + copg2-binary
# - psy + copg2
# - async + pg
# - 任何 DB driver
